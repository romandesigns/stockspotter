/target
.env
.env.*
!.env.example

# Python
__pycache__/
*.pyc
.venv/
venv/

# JS/TS workspaces (apps/*, packages/*)
node_modules
dist
dist-ssr
*.local
.expo/

# Pi deploy state (see deploy.sh) — local to the deploy host, not source
.deployed-commit

# Tauri updater signing key — private, never commit. Stored as GitHub
# Actions secrets (TAURI_SIGNING_PRIVATE_KEY / _PASSWORD) for CI signing.
apps/client/src-tauri/*.key
apps/client/src-tauri/target

# Editor
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
